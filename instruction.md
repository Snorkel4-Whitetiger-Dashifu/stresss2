Bring the outage-routing deployment back online after a failed pager-policy rollout. The rollout left the responder host in a bad state: the operator wrapper at `/usr/local/bin/compile-outages` is broken and not executable, a stale compile lock is wedged under `/var/lock`, the cron schedule is gone, the dedicated service account was never provisioned, the output directory permissions are wrong, and the compiler itself is producing unsafe responder queues. `/app/docs/deployment_runbook.md` defines the required deployment state — account, wrapper and its lock semantics, schedule, ownership and modes; restore the host to match it.

Then restore the compiler's output. Keep the interface `python3 /app/workflow/compile_outages.py --input PATH --output-dir PATH` and the defaults `/app/data/outages.json` and `/app/output`. The compile reconciles incidents with the maintenance, response-policy, routing-exception, handoff, blackout, and degradation records under `/app/data`, canonicalizes incident state, builds unplanned service windows, applies the routing domains in operational order, and deterministically calculates inclusion thresholds, priorities, signatures, and checksums. It must remain idempotent and work with alternate incident inputs.

Produce exactly `downtime_summary.json`, `service_windows.json`, and compact `incident_queue.jsonl`. `/app/docs/routing_contract.json` is the output contract — source paths, schemas, exact key sets, identifier payloads, and checksum encodings; match it exactly. Summary values must be SHA-reproducible, not merely schema-compatible, following the contract's exact UTF-8 payload fields, delimiters, boolean encodings, and complete policy-line serialization. The summary fields derived from planned incidents — `canonical_incident_count`, `severity_counts`, `planned_excluded_count`, and `canonical_input_checksum` — never surface in the window or queue outputs; the contract's `summary_field_recovery` block gives a fully worked example so you can verify those fields directly rather than against a hidden fixture.

How the compile actually behaves is not in the contract. That was settled by the change-advisory board over months, and the record is `/app/incident/rollout_review_log.md` — normalization, dedupe, interval semantics, policy resolution against the approved defaults, attenuation, the responder debt ledger, thresholds, scoring, priority, and queue ordering all live there as CAB-ticketed entries scattered through the review archive. Most of the log is routine noise, and the February draft proposals were partly reversed later; where a draft proposal and a later decision disagree, the later decision governs.

`source_incident_ids` is a sorted array of unique string IDs in both `service_windows.json` and `incident_queue.jsonl`; comma-joined incident IDs are used only inside signature/checksum payloads.

Preserve `/app/workflow/.compile_outages.original` unchanged. Do not read or import verifier artifacts, and do not place the forbidden verifier path or fixture/test filenames listed in the contract anywhere in repaired source code, including comments and docstrings.
