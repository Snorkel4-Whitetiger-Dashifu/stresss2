Bring the outage-routing deployment back online after a failed pager-policy rollout. The rollout left the responder host in a bad state: the dedicated service account, the operator wrapper at `/usr/local/bin/compile-outages`, the compile lock under `/var/lock`, the cron schedule, the output directory ownership and permissions, the compile log directory under `/var/log/outage-compile` and its rotation drop-in are all missing or wrong, and an unrotated leftover from the crashed run is still sitting on disk. `/app/docs/deployment_runbook.md` defines the deployment state you need to restore: provision the account with no interactive shell, reinstate the schedule under that account, correct the ownership and modes, prune the rollout leftover, clear the stale lock, and install rotation so the scheduled compile both runs and rotates as the service account rather than as root.

The same rollout shipped a compiler build that produces unsafe responder queues, so restoring the host is not enough on its own. Return `/app/workflow/compile_outages.py` to the behaviour the change-advisory board signed off, keeping its interface `--input PATH --output-dir PATH` with defaults `/app/data/outages.json` and `/app/output`, so that it writes `downtime_summary.json`, `service_windows.json` and compact `incident_queue.jsonl`. It has to stay idempotent and keep working on a different incident input. `/app/docs/routing_contract.json` is the output contract — source paths, schemas, exact key sets, identifier payloads and checksum encodings — and your output must match it exactly, down to the byte-level payloads it specifies.

How the compile is meant to behave is not in the contract. The change-advisory board settled it over several months, and `/app/incident/rollout_review_log.md` is the record: normalization, dedupe, interval semantics, policy resolution, attenuation, the responder debt ledger, thresholds, scoring, priority and queue ordering are all CAB-ticketed entries in there. Where two entries disagree, the later dated decision governs. Leave `/app/workflow/.compile_outages.original` unchanged, and do not read or reference verifier artifacts anywhere in the repaired source, including comments.
